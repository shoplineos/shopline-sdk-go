// Package membersystem generated by SHOPLINE since v20251201
package membersystem

import (
	"github.com/shoplineos/shopline-sdk-go/client"
)

func (e *GiftCardUpdateEvent) GetSupportedTopic() string {
	return "app_memberSystem_giftCard/challenged"
}

type GiftCardUpdateEvent struct {
	client.WebhookEvent
	/*
	   Update time
	   Example: 1.721829526566e+12
	*/
	UpdatedAt int64 `json:"updated_at,omitempty"`

	/*
	   The balance of gift card after the change.
	   Accurate to two decimal places, for example, $10.00 will return 1000.
	   Example: 1000
	*/
	CurrentBalance int64 `json:"current_balance,omitempty"`

	/*
	   The original value of gift card
	   Example: 10000
	*/
	GiftCardOriginalValue int64 `json:"gift_card_original_value,omitempty"`

	/*
	   store ID
	   Example: 1718263946864
	*/
	StoreId string `json:"store_id,omitempty"`

	/*
	   Gift card update type.
	   DECREASE: Gift card decrease
	   INCREASE: Gift card increase
	   Example: DECREASE
	*/
	UpdateType string `json:"update_type,omitempty"`

	/*
	   The absolute value of the gift card amount for this update.
	   Accurate to two decimal places, for example, $10.00 will return 1000.
	   Example: 1000
	*/
	UpdateValue int64 `json:"update_value,omitempty"`

	/*
	   Customer id
	   Example: 2404721679
	*/
	BuyerId string `json:"buyer_id,omitempty"`

	/*
	   Gift card code
	   Example: 240acd
	*/
	GiftCardCode string `json:"gift_card_code,omitempty"`

	/*
	   Gift card expiration time
	   Example: 1.721829526566e+12
	*/
	GiftCardExpiryDate int64 `json:"gift_card_expiry_date,omitempty"`

	/*
	   Gift card update scenario type.
	   SELF_PURCHASE: Purchase for the customers themselves.
	   OTHERS_SEND: Gift from others
	   PURCHASE_PAYMENT: Customers use gift cards to pay for shopping.
	   RETURN_INCREASE: When the merchant refunds an order, the amount used is added back to the gift card.
	   CARD_DIABLED: The gift card is disabled.
	   Example: SELF_PURCHASE
	*/
	SceneType string `json:"scene_type,omitempty"`
}
