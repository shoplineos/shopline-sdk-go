// Package application generated by SHOPLINE since v20251201
package application

import (
	"github.com/shoplineos/shopline-sdk-go/client"
)

func (e *AppInstallationStatusChangedEvent) GetSupportedTopic() string {
	return "apps/installed_uninstalled"
}

type AppInstallationStatusChangedEvent struct {
	client.WebhookEvent
	/*
	   The current display name of the store (modifiable by the merchant).
	   Example: SHOPLINE Store
	*/
	Name string `json:"name,omitempty"`

	/*
	   The type of operation being performed. For example:
	   + `INSTALL`: App installation
	   + `UNINSTALL`: App uninstallation
	   Example:
	*/
	Operate string `json:"operate,omitempty"`

	/*
	   The time when the event occurred, in the format of a 13-digit milliseconds timestamp.

	   :::note{title="note"}
	   The `created_at` and `timestamp` fields represent the same moment in time to meet different developer needs.
	   :::
	   Example: 1.293941471e+12
	*/
	Timestamp int64 `json:"timestamp,omitempty"`

	/*
	   The [IANA time zone](https://data.iana.org/time-zones/tzdb-2021a/zone1970.tab) identifier used by the store.

	   For example:
	   + `America/New_York`: Eastern Time (US)
	   + `Asia/Tokyo`: Japan Standard Time
	   Example:
	*/
	Timezone string `json:"timezone,omitempty"`

	/*
	   The store's unique identifier in SHOPLINE, corresponding to the `{handle}` part of the domain name `{handle}.myshopline.com`.
	   Example: smartstore
	*/
	Handle string `json:"handle,omitempty"`

	/*
	   The global unique identifier for the store.
	   Example: 1610418123456
	*/
	StoreId string `json:"store_id,omitempty"`

	/*
	   The globally unique identifier for the app.
	   Example: 35f91ba37b941e6ce5fcc67975ab7aafb0a12345
	*/
	Appkey string `json:"appkey,omitempty"`

	/*
	   The [ISO 3166-1](https://www.iso.org/iso-3166-country-codes.html) standard code for the country or region where the store is located.
	   Example: US
	*/
	CountryCode string `json:"country_code,omitempty"`

	/*
	   The exact date and time when the event occurred. Format: [ISO 8601](https://www.iso.org/iso-8601-date-and-time-format.html). The time zone corresponds to the store's time zone.

	   :::note{title="note"}
	   The `created_at` and `timestamp` fields represent the same moment in time to meet different developer needs.
	   :::
	   Example: 2024-04-01T14:01:23+08:00
	*/
	CreatedAt string `json:"created_at,omitempty"`

	/*
	   The global unique identifier for the merchant.
	   Example: 2000001234
	*/
	MerchantId string `json:"merchant_id,omitempty"`
}
