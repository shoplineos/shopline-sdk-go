// Package product generated by SHOPLINE since v20251201
package product

import (
	"github.com/shoplineos/shopline-sdk-go/client"
)

func (e *UpdateTheVariantPricingRulesInTheCatalogEvent) GetSupportedTopic() string {
	return "company/action"
}

type QuantityPriceBreak struct {
	/*
	   Gradient-based minimum purchase quantity.
	   Example: 2
	*/
	MinimumQty int `json:"minimum_qty,omitempty"`

	/*
	   The adjustment value based on `percent_type`, formatted as a percentage.
	   - When decreasing the price, the value is limited to 0-100.
	   - When increasing the price, the value is limited to 0-10,000.
	   Example: 10
	*/
	Percent int `json:"percent,omitempty"`

	/*
	   The type of percentage adjustment, with a value of `1` for decreasing the price and a value of `2` for increasing the price.
	   Example: 1
	*/
	PercentType int `json:"percent_type,omitempty"`

	/*
	   Fixed price, valid only when `price_adjustment_method=2`.
	   Example: 100
	*/
	Price string `json:"price,omitempty"`
}

type UpdateTheVariantPricingRulesInTheCatalogEvent struct {
	client.WebhookEvent
	/*
	   Variant pricing rules in the catalog.
	   Example: map[catalog_id:6505476185111667646 percent:10 percent_type:1 price:100 price_adjustment_method:1 product_id:16064679140646857045521690 quantity_price_breaks:[] quantity_rules:map[increment:2 maximum:100 minimum:2] variant_id:18064419722745311498141690]
	*/
	CatalogVariantPriceRule CatalogVariantPriceRule `json:"catalog_variant_price_rule,omitempty"`
}

type CatalogVariantPriceRule struct {
	/*
	   The adjustment value based on `percent_type`, formatted as a percentage.
	   - When decreasing the price, the value is limited to 0-100.
	   - When increasing the price, the value is limited to 0-10,000.
	   Example: 10
	*/
	Percent int `json:"percent,omitempty"`

	/*
	   The method of price adjustment, with a value of `1` for percentage and a value of `2` for fixed price (default).
	   Example: 1
	*/
	PriceAdjustmentMethod int `json:"price_adjustment_method,omitempty"`

	/*
	   Rules for purchase quantity
	   Example: map[increment:2 maximum:100 minimum:2]
	*/
	QuantityRules QuantityRules `json:"quantity_rules,omitempty"`

	/*
	   The type of percentage adjustment, with a value of `1` for decreasing the price and a value of `2` for increasing the price.
	   Example: 1
	*/
	PercentType int `json:"percent_type,omitempty"`

	/*
	   Fixed price, valid only when `price_adjustment_method=2`.
	   Example: 100
	*/
	Price string `json:"price,omitempty"`

	/*
	   The ID of the product in the catalog.
	   Example: 16064679140646857045521690
	*/
	ProductId string `json:"product_id,omitempty"`

	/*
	   Gradient pricing list.
	   Example: []
	*/
	QuantityPriceBreaks []QuantityPriceBreak `json:"quantity_price_breaks,omitempty"`

	/*
	   The ID of the variant in the catalog.
	   Example: 18064419722745311498141690
	*/
	VariantId string `json:"variant_id,omitempty"`

	/*
	   The ID of company location catalog.
	   Example: 6505476185111667646
	*/
	CatalogId string `json:"catalog_id,omitempty"`
}

type QuantityRules struct {
	/*
	   Purchase quantity increment where each increase must be a multiple of the increment.
	   Example: 2
	*/
	Increment int `json:"increment,omitempty"`

	/*
	   Maximum purchase quantity.
	   Example: 100
	*/
	Maximum int `json:"maximum,omitempty"`

	/*
	   Minimum purchase quantity.
	   Example: 2
	*/
	Minimum int `json:"minimum,omitempty"`
}
