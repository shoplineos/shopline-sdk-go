// Package metafield generated by SHOPLINE since v20260301
package metafield

import (
	"errors"
	"fmt"

	"github.com/shoplineos/shopline-sdk-go/client"
)

type GetTheMetafieldCountForAResourceAPIReq struct {
	client.BaseAPIRequest
	/*
	   The resource ID to which the metafield belongs. For example, the ID of product A is `1605898661`, when a new metafield is added to product A, the `owner_id` is `1605898661`.
	   Example: 16015223514512351235
	*/
	OwnerId string `url:"-" json:"-"` // Required
	/*
	   The type of the resource to which the metafield belongs. Valid values are:
	   - `products`: products
	   - `variants`: product variants
	   - `collections`: product collections
	   - `customers`: customers
	   - `orders`: orders
	   - `pages`: pages
	   - `blogs`: blogs collections
	   - `articles`: blogs
	   - `shop`: store
	*/
	Resource string `url:"-" json:"-"` // Required
}

func (req *GetTheMetafieldCountForAResourceAPIReq) GetEndpoint() string {

	return fmt.Sprintf("%s/%s/metafields/count.json", req.Resource, req.OwnerId)

}

func (req *GetTheMetafieldCountForAResourceAPIReq) GetMethod() string {
	return "GET"
}

// Verify the api request parameters
func (req *GetTheMetafieldCountForAResourceAPIReq) Verify() error {

	if req.OwnerId == "" {
		return errors.New("OwnerId is required")
	}

	if req.Resource == "" {
		return errors.New("Resource is required")
	}

	return nil
}

func (req *GetTheMetafieldCountForAResourceAPIReq) NewAPIResp() *GetTheMetafieldCountForAResourceAPIResp {
	return &GetTheMetafieldCountForAResourceAPIResp{}
}

type GetTheMetafieldCountForAResourceAPIResp struct {
	client.BaseAPIResponse
	/*
	   The number of metafields.
	*/
	Count int `json:"count,omitempty" url:"-"`
}
